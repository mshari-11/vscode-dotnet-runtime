{
  "name": "location-link-tracker",
  "version": "1.0.0",
  "description": "Simple consent-based link that requests geolocation and reports it to a server.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
